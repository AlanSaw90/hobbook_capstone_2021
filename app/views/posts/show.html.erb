
<section class='single-post-show'>
  <% if @post.photo.attached? %>
    <%= image_tag @post.photo, style: 'width: 200px;' %>
  <% end %>
    <%= @post.caption %>

    <div class="comment-form">
        <%= form_with(model: [@
        <% end %>    
    </div>    
</section>