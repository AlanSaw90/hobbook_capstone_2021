



  <section class='navbar'>
  <%= link_to 'Hobbook', root_path %>

  <% if user_signed_in? %>
    <%= link_to 'Profile', user_path(current_user) %>
    <%= link_to 'Setting', edit_user_path(current_user) %>
    <%= button_to 'Logout', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?'} %>
  <% else %>
    <%= link_to 'Sign Up', new_user_registration_path %>
    <%= link_to 'Login', new_user_session_path %>
  <% end %>
</section>



<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href=<%= root_path %> >Hobbook</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href= <%=root_path %> >Home</a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href= <%=user_path(current_user)%> >Profile</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href= <%= edit_user_path(current_user)%> >Setting</a>
          </li>
          <li class="nav-item">
            
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?'} %>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link" href= <%= new_user_registration_path %> >Sign Up</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href= <%= new_user_session_path %> >Login</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
